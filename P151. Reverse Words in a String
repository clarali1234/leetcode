class Solution(object):
    def reverseWords(self, s):
       
        l = s.split()
        res = []
        for i in range(len(l) - 1, -1, -1):
            res.append(l[i])
        return ' '.join(res)   
