

1. 在一个array中寻找 长度大小为k的subarray 满足以下条件： 1 subarray中的元素和是最大， 2. subarray中没有重复元素

def max_subarray_sum(nums, k):
    left = 0
    seen = set()
    current_sum = 0
    max_sum = 0

    for right in range(len(nums)):

        # 去重
        while nums[right] in seen:
            seen.remove(nums[left])
            current_sum -= nums[left]
            left += 1

        seen.add(nums[right])
        current_sum += nums[right]

        # 控制窗口 <= k
        if right - left + 1 > k:
            seen.remove(nums[left])
            current_sum -= nums[left]
            left += 1

        if right - left + 1 == k:
          max_sum = max(max_sum, current_sum)

    return max_sum

