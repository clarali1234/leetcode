# worst case, 1 1 1 1 0 1 1, we can do it in log(n)

class Solution:
    def findMin(self, nums: List[int]) -> int:
        min = nums[0]
        for num in nums:
            if num < min:
                min = num
        return min
