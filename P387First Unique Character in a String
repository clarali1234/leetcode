# time O(len(s))
# space O(len(s))
class Solution:
    def firstUniqChar(self, s: str) -> int:
        ch = []
        for i in set(s):
            if s.count(i) == 1:
                ch.append(s.index(i))
        if len(ch) > 0:
            return min(ch)
        return -1
