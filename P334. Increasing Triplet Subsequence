# 思路 greedy, maintain min1, min2, min1 <= min2

 1    min1     2     min 2   3
 
 1. min1 = num
 2. min2 = mum
 3. return True


# time O(n)
# space O(1)


class Solution(object):
    def increasingTriplet(self, nums):
        
        min1 = sys.maxsize
        min2 = sys.maxsize
        
        for num in nums:
            if num <= min1:
                min1 = num
            elif num <= min2:
                min2 = num
            else:
                return True
        return False
