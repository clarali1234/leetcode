# time O(n)
# space O(1)
class Solution(object):
    def increasingTriplet(self, nums):
        first = second = sys.maxsize
        for n in nums:
            if n <= first:
                first = n
            elif n <= second:
                second = n
            else:
                return True
        return False
