我们可以用三个矩阵分别检查三个规则是否有重复数字，比如用row, col, block分别检查行、列、块是否有重复数字，代码如下：

# time O(n^2)
# space O(n)

class Solution(object):
    def isValidSudoku(self, board):
        """
        :type board: List[List[str]]
        :rtype: bool
        """
        seen = []
        for i in range(9):
            for j in range(9):
                if board[i][j] == '.':
                    continue
                cur = board[i][j]
                if (i, cur) in seen or (cur, j) in seen or (i//3, j//3, cur) in seen:
                    return False
                seen.append((i, cur))
                seen.append((cur, j))
                seen.append((i//3, j//3, cur))
        return True
        
